#action {%0 lies down and falls asleep.}
{
    #if {$MP > $MIN_MANA_TO_KEEP && "$LEADER" == %0 && "$CAN_HEAL" == "TRUE"}
    {
        cure %0;
        mmm;
    };
}

#nop action {It's already empty} {give canteen "$LEADER"}